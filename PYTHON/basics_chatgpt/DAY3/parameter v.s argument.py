# def add(a, b):   # parameters
#     return a + b

# add(2, 3)        # arguments

# def func(a=1, b=5):
#     print(a, b)

# func() # arguments are not provided, so default values will be used

def add(a, b):
    print(a + b)

result = add(2, 3)
print(result)
# In the above code, the add function does not return any value, so it returns None by default. When we print the result of add(2, 3), it will print None.